# TODO

Hook alert method and change to custom message when license isnt active, prevent
from going into exam mode

# TODO mit website
Connect to websocket server (durable objects cloudflare workers)
Get all clients via function of durable object
be able to view logs of client and entered keys etc